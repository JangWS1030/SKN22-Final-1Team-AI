════════════════════════════════════════════════════════════════════════════════
🚀 Runpod 배포 가이드
════════════════════════════════════════════════════════════════════════════════

【빌드 완료 확인】
✓ GitHub Actions: 완료됨
✓ Docker 이미지: lecreek/mirrai-sd-app:latest (Docker Hub에 업로드됨)
✓ 코드 변경사항: 
  - front=down 앞머리 복원 범위 대폭 확대
  - bangs_requested 파라미터 추가
  - 마스크 복원 범위: 2.5배 확대

════════════════════════════════════════════════════════════════════════════════
【배포 순서】
════════════════════════════════════════════════════════════════════════════════

【1단계】기존 Pod 확인 및 종료
────────────────────────────────────────────────────────────────────────────
# 기존 활성 Pod 목록 확인
$ runpod pod list | grep mirrai-sd

# 기존 Pod 종료 (필요한 경우)
$ runpod pod terminate <OLD_POD_ID>

【2단계】새 Pod 배포 (A100 GPU 권장)
────────────────────────────────────────────────────────────────────────────
$ runpod pod run \
  --name mirrai-sd-app-$(date +%s) \
  --image lecreek/mirrai-sd-app:latest \
  --container-disk 20 \
  --gpu '1x A100' \
  --volume-size 50 \
  --exposed-ports '5000'

# Pod ID 메모: ____________________

【3단계】Pod 상태 확인
────────────────────────────────────────────────────────────────────────────
# Pod 상태 조회
$ runpod pod status <NEW_POD_ID>

# Pod IP 주소 확인
$ runpod pod describe <NEW_POD_ID> | grep -i "ip\|url"

【4단계】헬스 체크
────────────────────────────────────────────────────────────────────────────
# API 서버 정상 작동 확인
$ curl -X GET http://<POD_IP>:5000/health

# 응답 예시:
# {"status": "healthy", "model": "mirrai-sd-app"}

【5단계】모델 테스트: 앞머리 요청 (down fringe)
────────────────────────────────────────────────────────────────────────────
# 테스트 요청
$ curl -X POST http://<POD_IP>:5000/api/generate \
  -H 'Content-Type: application/json' \
  -d '{
    "hairstyle_text": "male haircut, soft two-block, down fringe",
    "color_text": "brown",
    "subject_gender": "male",
    "prompt_context": {
      "style_axes": {
        "front": "down",
        "parting": "non_parted"
      }
    }
  }' \
  -o test_result.json

【6단계】결과 검증
────────────────────────────────────────────────────────────────────────────
# 생성된 이미지 다운로드
$ curl -X GET http://<POD_IP>:5000/api/result/<REQUEST_ID> -o result.jpg

# 이미지 시각 확인:
# - 앞머리(fringe)가 제대로 생성되었는지 확인
# - 기존 앞머리가 제거되지 않았는지 확인
# - 전체 이미지 화질 정상 여부 확인

════════════════════════════════════════════════════════════════════════════════
📋 코드 변경 사항 (커밋: f72661d)
════════════════════════════════════════════════════════════════════════════════

【문제】
- 사용자가 "front=down" + "down fringe"로 앞머리 명시 요청
- 생성된 이미지에서 기존 앞머리가 제거되면서 이미지 깨짐
- "down fringe"인데 앞머리가 없거나 크롭됨

【원인】
- _build_bangs_recovery_mask() 함수의 복원 범위가 고정되어 있음
- front=down 요청 여부 관계없이 동일 범위 적용
- 남성 short: band_bottom = forehead_y + 0.22 * face_h (이마 22% 범위만)

【수정】
1. bangs_requested 파라미터 추가
2. 복원 범위 대폭 확대:
   - Male short: 0.22 → 0.56 (2.5배)
   - Male medium: 0.18 → 0.48 (2.7배)
   - Female short: 0.28 → 0.62 (2.2배)
3. Center half 너비 증가
4. Component 필터 완화
5. Dilation kernel 크기 증가

【효과】
✓ front=down 요청 시 앞머리 보존
✓ down fringe 이미지 깨짐 현상 해결
✓ 기존 앞머리 복원 범위 폭 확대

════════════════════════════════════════════════════════════════════════════════
🔍 모니터링 체크리스트
════════════════════════════════════════════════════════════════════════════════

배포 완료 후 확인 사항:

[ ] Pod 생성 완료 (POD_ID: __________________)
[ ] Pod IP 확인 (IP: __________________)
[ ] 헬스 체크 성공 (/health 응답 200)
[ ] 앞머리 테스트 요청 성공
[ ] 생성 이미지에서 앞머리 확인됨
[ ] 이미지 품질 확인 (깨짐 현상 없음)
[ ] 전체 시스템 정상 작동 확인

════════════════════════════════════════════════════════════════════════════════
